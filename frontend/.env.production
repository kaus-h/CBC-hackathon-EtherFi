# Production Environment Variables
# These are used when building the frontend for Railway deployment

# API URL - uses relative path since backend serves frontend
VITE_API_URL=/api

# WebSocket URL - connects to the same Railway domain
VITE_WS_URL=https://etherfi-anomanly.up.railway.app
